import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'myAccounts',
  webDir: '../../dist/apps/my-accounts',
  bundledWebRuntime: false,
};

export default config;
